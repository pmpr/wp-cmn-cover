window.addEventListener('DOMContentLoaded', initGoogleAnalytics);

function initGoogleAnalytics() {

    window.dataLayer = window.dataLayer || [];
    function gtag() {
        dataLayer.push(arguments);
    }
    gtag('js', new Date());
    gtag('config', '{{ analytics_id|esc_attr }}');
}

